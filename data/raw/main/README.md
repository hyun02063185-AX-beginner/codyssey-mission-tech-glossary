# Main Course Raw Data

본과정 미션에서 추출한 기술용어 원천 데이터를 보존하는 폴더입니다.

## 관리 원칙

- M01 Pilot으로 공통 추출 형식을 먼저 검증합니다.
- 이후 M02~M13에도 `extraction-template.md`를 동일하게 사용합니다.
- 미션별 raw 파일은 각 `mXX/` 폴더에 독립적으로 보존합니다.
- 중복 제거와 표준화는 `data/curated/`에서 수행합니다.
- `direct`, `required`, `related` 출처 상태를 섞지 않습니다.
