# defer

## 한 줄 설명

`defer`는 외부 script를 HTML 파싱과 병렬로 내려받고, 문서 파싱이 끝난 뒤 문서에 적힌 순서대로 실행하게 하는 `script` 속성입니다.

## 쉽게 설명하면

"다운로드는 미리, 실행은 나중" — 페이지 뼈대가 다 만들어질 때까지 기다렸다가 순서대로 실행하는 약속입니다.

## 정확한 설명

`defer` 속성이 있는 외부 스크립트(`script src="..."`)는 다운로드를 HTML 파싱과 병렬로 진행해 파싱을 막지 않습니다. 실행은 문서 파싱이 완료된 뒤, DOMContentLoaded 이벤트가 발생하기 전에 이루어집니다. defer 스크립트가 여러 개면 문서에 나타난 순서대로 실행됩니다. inline script에는 효과가 없으며, module script는 속성과 무관하게 기본적으로 지연 실행됩니다. 일반 script가 파싱 중 다운로드·실행으로 파서를 막는 것과 대비됩니다.

## 동작 원리

- **기본 script**: 다운로드와 실행이 끝날 때까지 HTML 파싱이 중단됩니다(파서 차단).
- **defer**: 파싱을 계속하며 스크립트를 병렬 다운로드 → 문서 파싱 완료 → defer 스크립트들을 문서 순서대로 실행 → DOMContentLoaded 발생.
- **async**: 병렬 다운로드 후 다운로드가 끝나는 즉시 실행됩니다. 실행 순서는 보장되지 않고, 실행 중에는 파싱이 중단됩니다.
- async와 defer가 함께 있으면 async가 우선합니다.

## 이 미션에서는 왜 필요한가

M01에서 파일을 역할별로 분리(js/menu.js, js/theme.js 등)하면서, head에서 로드해도 메뉴·폼·테마 코드가 초기 DOM을 안정적으로 읽을 수 있게 합니다. JavaScript가 `querySelector`로 요소를 찾을 때 "아직 요소가 없다"는 문제를 줄입니다.

## 코드 예

```html
<head>
  <script src="js/menu.js" defer></script>
  <script src="js/theme.js" defer></script>
</head>
```

파싱이 끝난 뒤 menu.js → theme.js 순서로 실행되므로 두 스크립트 모두 DOM 요소를 안전하게 조회할 수 있습니다.

## 주의할 점 / 경계 조건

- inline script에는 defer가 동작하지 않습니다. `src` 속성이 있는 외부 스크립트에만 의미가 있습니다.
- defer 스크립트의 실행은 파싱 완료 후이므로, 그보다 먼저 실행되어야 하는 코드가 있다면 배치를 바꿔야 합니다.
- module script는 defer 없이도 지연 실행되지만, 일반 script의 defer와 섞어 설명하지 않아야 합니다.
- defer 스크립트가 로드에 실패하면 DOMContentLoaded가 지연됩니다(문서 순서 실행을 기다리기 때문).

## 흔한 오해

`defer`가 스크립트를 실행하지 않게 하는 옵션이라고 생각합니다. 실행은 하되 시점만 문서 파싱 후로 미루는 것입니다. "script를 만나면 파싱이 멈춘다"는 기본 동작을 defer가 풀어줍니다.

## 비슷한 개념과의 차이

- **async**: 다운로드 완료 즉시 실행, 순서 미보장 — 다른 스크립트와 독립적인 경우에 적합합니다.
- **기본(속성 없음)**: 파서 차단 — 문서에서의 위치가 중요합니다.
- **module script**: import/export를 지원하며 기본적으로 지연 실행됩니다.

## 관련 용어

- html
- javascript
- dom

## 동료평가 질문

head에서 defer 없이 스크립트를 실행하면 어떤 요소가 아직 없을 수 있나요? defer 스크립트 두 개의 실행 순서는 무엇으로 결정되나요?
